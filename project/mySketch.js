
let myMessage = "Welcome to Lick Vegas."
let xLocation = 30; 

function setup() {
  createCanvas(400, 400);
   background(50);
   textSize (50);
  noStroke();
  fill(255);
  text(myMessage, 50, 200);
  xLocation = xLocation + 1;
 
}
function draw() {
 
}
